# ClipPilot MVP 功能清单

## 已完成功能

### 1. 基础桌面悬浮形态

- 程序启动后显示悬浮窗。
- 默认只显示卡通人物，不显示气泡。
- 使用项目内置卡通人物图片：`copyutil/Assets/Images/clip_pilot_mascot.png`。
- 窗口无边框、透明背景、始终置顶。
- 窗口可拖动。
- 拖拽文件时显示临时提示气泡。
- 有文件、文件夹、截图或路径内容时，人物左上角弹出操作气泡。
- 操作按钮已改为纵向排列。

### 2. 文件 / 文件夹拖拽

- 支持拖入单个文件。
- 支持拖入多个文件。
- 支持拖入文件夹。
- 支持文件和文件夹混合拖入。
- 拖入后复制路径到剪贴板。
- 多个路径按多行文本复制。
- 拖入后弹出气泡操作。

### 3. 剪贴板文本路径识别

- 支持监听剪贴板变化。
- 使用 Windows 剪贴板序列号，只处理新变化。
- 启动时不处理历史剪贴板内容。
- 支持识别真实存在的文件路径。
- 支持识别真实存在的文件夹路径。
- 支持多行路径。
- 支持带英文引号、中文引号的路径。
- 支持 `file:///` 路径。
- 支持网络共享路径。
- 路径不存在时忽略，不弹窗。

### 4. 剪贴板文件列表识别

- 支持从资源管理器复制文件后识别。
- 支持从资源管理器复制文件夹后识别。
- 支持多个文件 / 文件夹。
- 识别后弹出气泡操作。

### 5. 剪贴板图片识别

- 支持检测剪贴板图片类型。
- 只有剪贴板内容是图片时才生成图片文件。
- 图片保存到系统临时目录：`%TEMP%\ClipPilot\`。
- 保存格式为 PNG。
- 已修复部分截图工具 / 微信截图保存空白的问题：保存前会强制转换像素格式并拷贝像素。
- 截图保存后可在气泡中复制路径或复制文件。

### 6. 气泡操作

- 复制文件：将真实文件对象写入剪贴板。
- 复制路径：将文件 / 文件夹路径写入剪贴板。
- 复制名称：复制文件名或文件夹名。
- 打开：打开文件。
- 打开目录：打开文件所在目录，或打开文件夹。
- 文件夹场景下隐藏“打开”按钮。

### 7. 托盘能力

- 系统托盘图标。
- 托盘菜单支持显示悬浮窗。
- 托盘菜单支持隐藏悬浮窗。
- 托盘菜单支持退出程序。
- 关闭窗口时最小化/隐藏到托盘。

### 8. 状态保存

- 保存悬浮窗位置到 `%AppData%\ClipPilot\settings.json`。
- 启动后恢复上次悬浮窗位置。

## 待完成功能

### 高优先级

1. 优化气泡展示和隐藏逻辑。
   - 操作完成后是否自动隐藏气泡。
   - 点击人物是否展开 / 收起气泡。
   - 鼠标离开后是否自动收起。

2. 补充截图保存后的验证体验。
   - 保存失败时提示。
   - 保存成功后可选打开截图文件。
   - 临时截图文件清理策略。

3. 优化剪贴板监听策略。
   - 目前为 500ms 轮询。
   - 后续可改为 Windows 消息监听，降低资源占用。

4. 完善按钮在不同内容类型下的显示。
   - 文件：复制文件、复制路径、复制名称、打开、打开目录。
   - 文件夹：复制路径、复制名称、打开文件夹。
   - 截图：复制文件、复制路径、打开、打开目录。
   - 多文件：复制文件、复制路径、复制名称、打开目录。

### 中优先级

1. 增加设置入口。
   - 是否启用剪贴板监听。
   - 是否启用拖拽复制。
   - 是否显示成功提示。

2. 增加开机启动设置。

3. 增加更多复制格式。
   - Markdown 链接。
   - Markdown 图片。
   - HTML 图片标签。
   - JSON 数组。

4. 图片路径预览。

5. 快捷键。

### 低优先级 / 后续版本

1. 剪贴板历史。
2. JSON 格式化。
3. URL 识别。
4. 颜色识别。
5. OCR。
6. 批量重命名。
7. VSCode / Cursor / 终端打开。
8. 开发者路径转换。
9. 插件系统。

## 当前 MVP 目标

当前 MVP 聚焦：

1. 默认显示一个卡通人物悬浮助手。
2. 有文件、文件夹、路径或截图时弹出左上角气泡。
3. 用户可以快速复制路径、复制文件、打开文件或打开目录。
4. 截图进入剪贴板时自动保存为临时图片文件，并可复制路径。

# ClipPilot MVP 开发约定

## 产品定位

ClipPilot 是一个 Windows 桌面端轻量悬浮路径助手。MVP 只解决一个核心问题：用户复制或拖入文件 / 文件夹路径后，可以快速完成复制路径、复制文件、打开文件、打开目录。

## MVP 必做范围

### P0 功能

1. 悬浮窗启动后显示、始终置顶、无边框、可拖动。
2. 支持拖入单个文件 / 多个文件 / 文件夹，并复制完整路径。
3. 多文件路径复制为多行文本。
4. 监听剪贴板文本变化。
5. 识别真实存在的文件路径和文件夹路径。
6. 支持清洗带引号路径、中文引号路径、`file:///` 路径、普通 Windows 路径、网络共享路径。
7. 路径不存在时不弹窗。
8. 相同内容不重复弹窗。
9. 工具自身写入剪贴板时不重复触发。
10. 文件路径操作：复制文件、复制路径、复制文件名、打开文件、打开所在目录。
11. 文件夹路径操作：复制路径、复制文件夹名、打开文件夹。
12. 托盘菜单：显示悬浮窗、隐藏悬浮窗、退出。
13. 操作成功 / 失败提示。
14. 记忆悬浮窗位置。

### P1 功能

1. 多文件路径转真实文件复制。
2. 拖拽高亮反馈。
3. 关闭窗口时最小化到托盘。
4. 启动后恢复上次悬浮窗位置。

## MVP 暂不做

以下功能不要在 MVP 阶段实现，避免扩大范围：

- 设置页
- 开机启动
- 图片预览
- 图片内容复制
- 剪贴板历史
- JSON 工具
- 颜色识别
- URL 识别
- OCR
- 批量重命名
- VSCode / Cursor / 终端打开
- Markdown 链接
- HTML 标签
- JSON 数组
- 相对路径
- import 路径
- 主题配置
- 快捷键
- 插件系统

## 推荐技术实现

- 使用 C# + WPF。
- 优先本地能力，不引入云服务。
- 剪贴板监听 MVP 可使用 DispatcherTimer 轮询，默认 500ms。
- 文件对象复制使用 `Clipboard.SetFileDropList`。
- 打开文件 / 目录使用 `ProcessStartInfo`，并启用 `UseShellExecute = true`。
- 打开文件所在目录并选中文件使用 `explorer.exe /select,"path"`。
- 悬浮窗位置保存到 `%AppData%\ClipPilot\settings.json`。

## 建议模块

- `Services/ClipboardMonitorService.cs`：监听剪贴板变化、防重复触发。
- `Services/ClipboardWriteService.cs`：写入文本剪贴板、文件对象剪贴板。
- `Services/PathDetectService.cs`：路径清洗、路径识别、文件 / 文件夹存在性判断。
- `Services/FileActionService.cs`：打开文件、打开目录、打开所在目录。
- `Services/TrayService.cs`：托盘图标和菜单。
- `Services/WindowStateService.cs`：保存和恢复悬浮窗位置。
- `Models/PathItem.cs`：路径识别结果。
- `Models/PathItemType.cs`：文件 / 文件夹类型。

## UI 原则

- 默认使用深色小卡片。
- 尺寸保持轻量，不遮挡屏幕。
- 默认状态显示 `ClipPilot`。
- 检测到路径后展开操作卡片。
- 拖入文件时显示高亮和“松开复制路径”。
- 成功提示短暂展示，例如“已复制路径”“已复制文件”。

## 验收标准

### 基础验收

- 程序可以启动。
- 启动后出现悬浮窗。
- 悬浮窗始终置顶并可拖动。
- 拖入单个文件后剪贴板变为该文件完整路径。
- 拖入多个文件后剪贴板变为多行路径。
- 拖入文件夹后剪贴板变为文件夹路径。
- 复制存在的文件路径后弹出操作卡片。
- 复制存在的文件夹路径后弹出操作卡片。
- 点击“复制文件”后可以在资源管理器或聊天软件中粘贴真实文件。
- 点击“复制路径”后剪贴板为路径文本。
- 点击“复制文件名”后剪贴板为文件名。
- 点击“打开文件”后系统打开该文件。
- 点击“打开目录”后资源管理器打开并定位文件。
- 可以通过托盘隐藏 / 显示悬浮窗。
- 可以通过托盘退出程序。

### 稳定性验收

- 相同路径不会反复弹窗。
- 工具自身写入剪贴板不会再次触发弹窗。
- 路径不存在时不弹窗。
- 剪贴板为空、不含文本或被占用时不崩溃。
- 网络路径不可访问时不崩溃。
- 拖入大量文件时不崩溃。
- 退出后托盘图标消失。

## 开发原则

- 不做大而全。
- 不做复杂 AI。
- 不做云同步。
- 不做账号系统。
- 不做跨平台。
- 优先 Windows。
- 优先本地能力。
- 优先稳定可用。
- 优先小而顺手。
- 不为后续版本提前增加复杂抽象。

# 帮读助手（ClipPilot）Windows 桌面端需求文档

## 1. 项目背景

在日常开发、测试、办公、设计素材整理过程中，经常会遇到以下场景：

- 从聊天记录、日志、Excel、数据库、文档中复制了一个文件路径，但想直接把这个文件发给别人；
- 想把桌面上的文件拖到一个小工具里，自动复制文件路径；
- 想快速复制文件名、文件夹路径、Markdown 链接、HTML 图片标签等格式；
- 想复制图片路径后直接预览图片；
- 想快速打开文件所在目录；
- 想把路径文本转换成 Windows 系统里的“真实文件复制”状态。

因此计划开发一款轻量级 Windows 桌面端悬浮工具，用于增强剪贴板和文件路径操作能力。

---

## 2. 项目名称

### 2.1 中文名称

暂定名称：

- 帮读助手
- 路径助手
- 悬浮助手
- 剪贴板助手
- 文件路径助手

### 2.2 英文名称

暂定名称：

- ClipPilot
- PathMate
- FloatClip
- ClipHelper

本文档暂以 **帮读助手（ClipPilot）** 作为项目名称。

---

## 3. 项目定位

帮读助手是一款运行在 Windows 桌面端的轻量级悬浮效率工具。

它的核心定位是：

> 自动识别剪贴板中的文件路径、文件夹路径、图片路径、文本内容，并通过悬浮窗提供快捷操作。

该工具不是传统意义上的剪贴板历史软件，也不是单纯的文件管理器，而是介于“剪贴板增强工具”和“文件路径助手”之间的小型效率工具。

---

## 4. 目标用户

### 4.1 开发人员

典型需求：

- 复制文件路径后快速打开项目目录；
- 复制代码文件路径后生成相对路径；
- 拖入文件生成 Markdown 链接；
- 快速复制 import 路径；
- 快速在 VSCode、Cursor、终端中打开目录。

### 4.2 测试人员

典型需求：

- 快速处理日志文件路径；
- 复制截图路径；
- 将路径转换为真实文件并发送；
- 快速打开测试文件所在目录。

### 4.3 UI 设计人员

典型需求：

- 拖入图片素材自动复制路径；
- 快速复制图片文件名；
- 图片路径预览；
- 批量复制素材路径；
- 批量生成 Markdown / HTML 引用。

### 4.4 办公用户

典型需求：

- 从文档中复制路径后直接发送文件；
- 快速复制文件名；
- 快速打开文件夹；
- 微信、QQ、企业微信、钉钉中快速粘贴文件。

---

## 5. 技术方案

### 5.1 技术栈

第一版推荐技术栈：

- C#
- WPF
- .NET 8
- Windows 桌面端

### 5.2 开发工具

推荐开发环境：

- Visual Studio 2022 Community
- .NET 8 SDK
- Git
- GitHub Desktop（可选）
- Cursor（可选，用于辅助编码）

### 5.3 为什么选择 WPF

选择 WPF 的原因：

1. WPF 对 Windows 桌面能力支持成熟；
2. 操作系统剪贴板非常方便；
3. 实现文件拖拽简单；
4. 实现始终置顶窗口简单；
5. 实现托盘程序方便；
6. 程序体积比 Electron 更轻；
7. 更适合做 Windows 小工具；
8. 第一版不需要跨平台，优先保证 Windows 体验。

### 5.4 暂不选择 Electron 的原因

暂不优先选择 Electron：

1. 程序体积较大；
2. 对 Windows 文件型剪贴板支持不如原生方便；
3. 悬浮窗、系统托盘、文件对象复制需要额外处理；
4. 对本项目来说有些重。

### 5.5 暂不选择 Tauri 的原因

暂不优先选择 Tauri：

1. 需要引入 Rust 学习成本；
2. 系统能力调用需要前后端通信；
3. 对第一版 MVP 来说复杂度偏高。

---

## 6. 产品核心价值

### 6.1 核心价值一：路径转文件

用户复制一个文件路径后，工具可以将该路径对应的真实文件写入剪贴板。

例如用户复制：

```text
D:\项目资料\接口文档.docx
```

工具识别后弹出：

```text
检测到文件路径：接口文档.docx

[复制文件] [复制路径] [打开文件] [打开目录]
```

点击“复制文件”后，用户可以直接在微信、QQ、企业微信、资源管理器中粘贴该文件。

### 6.2 核心价值二：拖入文件自动复制路径

用户把一个文件拖入悬浮窗后，工具自动复制该文件路径。

例如拖入：

```text
D:\素材\首页.png
```

剪贴板自动变为：

```text
D:\素材\首页.png
```

### 6.3 核心价值三：多格式复制

同一个文件可以复制为不同格式：

- 完整路径；
- 文件名；
- 文件夹路径；
- Markdown 链接；
- HTML 图片标签；
- JSON 数组；
- 相对路径；
- import 路径。

### 6.4 核心价值四：桌面悬浮快捷操作

工具常驻桌面，不需要打开完整软件界面，用户复制或拖入内容后立即通过悬浮面板完成操作。

---

## 7. 第一版 MVP 功能范围

第一版只做最核心、最稳定、最容易落地的功能。

### 7.1 MVP 功能列表

| 模块 | 功能 | 是否第一版实现 |
|---|---|---|
| 悬浮窗 | 始终置顶 | 是 |
| 悬浮窗 | 可拖动 | 是 |
| 悬浮窗 | 记忆位置 | 是 |
| 文件拖拽 | 拖入文件复制路径 | 是 |
| 文件拖拽 | 拖入多个文件复制路径 | 是 |
| 剪贴板监听 | 识别文件路径 | 是 |
| 剪贴板监听 | 识别文件夹路径 | 是 |
| 文件操作 | 路径转真实文件复制 | 是 |
| 文件操作 | 打开文件 | 是 |
| 文件操作 | 打开所在目录 | 是 |
| 路径格式化 | 复制完整路径 | 是 |
| 路径格式化 | 复制文件名 | 是 |
| 托盘 | 最小化到托盘 | 是 |
| 托盘 | 托盘退出 | 是 |
| 设置 | 开机启动 | 可选 |
| 设置 | 监听开关 | 可选 |

---

## 8. 功能需求详细说明

## 8.1 悬浮窗模块

### 8.1.1 功能说明

程序启动后，在桌面显示一个悬浮窗口。

悬浮窗用于：

- 接收文件拖拽；
- 展示剪贴板识别结果；
- 显示快捷操作按钮；
- 显示状态提示。

### 8.1.2 基础要求

悬浮窗应支持：

- 始终置顶；
- 无边框；
- 可拖动；
- 圆角；
- 半透明；
- 阴影；
- 可折叠；
- 可展开；
- 可隐藏；
- 可恢复。

### 8.1.3 默认形态

默认形态建议为一个小圆球或小卡片。

示例：

```text
📎
```

或者：

```text
ClipPilot
```

### 8.1.4 展开形态

当识别到路径或用户拖入文件后，悬浮窗展开为操作卡片。

示例：

```text
┌──────────────────────────────┐
│ 检测到文件路径                 │
│ 接口文档.docx                  │
│ D:\项目资料\接口文档.docx      │
│                              │
│ [复制文件] [复制路径]           │
│ [打开文件] [打开目录]           │
└──────────────────────────────┘
```

### 8.1.5 拖拽状态

当用户拖入文件时，悬浮窗应有高亮反馈。

示例：

```text
┌──────────────────────────────┐
│ 松开后复制文件路径              │
└──────────────────────────────┘
```

### 8.1.6 状态提示

操作完成后显示短提示。

示例：

```text
已复制路径
```

```text
已复制文件
```

```text
文件不存在
```

---

## 8.2 剪贴板监听模块

### 8.2.1 功能说明

程序运行期间监听系统剪贴板变化。

当用户复制文本后，工具读取剪贴板内容并判断内容类型。

### 8.2.2 支持识别类型

第一版支持：

- 文件路径；
- 文件夹路径；
- 带引号的路径；
- file 协议路径；
- 网络共享路径。

后续支持：

- 图片路径；
- URL；
- JSON；
- 颜色值；
- Markdown；
- 代码片段。

### 8.2.3 路径示例

普通文件路径：

```text
D:\test\a.png
```

带引号路径：

```text
"D:\test\a.png"
```

文件夹路径：

```text
D:\project\demo
```

网络共享路径：

```text
\\192.168.1.10\share\a.docx
```

file 协议路径：

```text
file:///D:/test/a.png
```

### 8.2.4 监听流程

```text
监听剪贴板变化
    ↓
读取剪贴板文本
    ↓
清洗文本内容
    ↓
判断是否为路径
    ↓
判断文件或文件夹是否存在
    ↓
弹出悬浮操作卡片
```

### 8.2.5 清洗规则

需要处理：

- 前后空格；
- 中文引号；
- 英文引号；
- 换行；
- file:/// 前缀；
- 反斜杠和斜杠；
- 路径中的转义字符。

### 8.2.6 防重复触发

需要记录上一次处理的剪贴板内容，避免重复弹窗。

规则：

- 相同内容不重复提示；
- 工具自身写入剪贴板时不重复触发；
- 用户取消后短时间内不重复提示。

---

## 8.3 文件路径识别模块

### 8.3.1 功能说明

判断剪贴板中的文本是否为有效路径。

### 8.3.2 判断规则

满足以下任意条件，可进入路径判断：

- 包含 Windows 盘符，如 `C:\`、`D:\`；
- 以 `\\` 开头，表示网络共享路径；
- 以 `file:///` 开头；
- 包含常见文件后缀；
- 路径存在于本地文件系统。

### 8.3.3 文件路径判断

使用：

```csharp
File.Exists(path)
```

### 8.3.4 文件夹路径判断

使用：

```csharp
Directory.Exists(path)
```

### 8.3.5 不存在路径处理

如果路径不存在：

第一版建议不弹窗，避免打扰用户。

后续可在设置中提供：

- 不存在路径也提示；
- 自动复制路径；
- 尝试打开上级目录。

---

## 8.4 路径转真实文件复制模块

### 8.4.1 功能说明

当用户复制的是文件路径时，可以点击“复制文件”，将该路径对应的文件对象写入系统剪贴板。

这相当于用户在资源管理器中选中文件并按下 Ctrl + C。

### 8.4.2 使用场景

用户复制：

```text
D:\资料\报告.pdf
```

点击“复制文件”。

然后可以粘贴到：

- 微信；
- QQ；
- 企业微信；
- 钉钉；
- Outlook；
- 资源管理器；
- 桌面；
- 其他支持文件粘贴的软件。

### 8.4.3 技术实现

WPF / C# 中可使用：

```csharp
StringCollection files = new StringCollection();
files.Add(@"D:\资料\报告.pdf");
Clipboard.SetFileDropList(files);
```

### 8.4.4 多文件支持

如果剪贴板中包含多行路径：

```text
D:\资料\a.pdf
D:\资料\b.docx
D:\资料\c.png
```

可以识别为多个文件，并批量写入剪贴板。

### 8.4.5 异常处理

需要处理：

- 文件不存在；
- 文件被占用；
- 路径无权限；
- 网络路径不可访问；
- 文件过大；
- 剪贴板被其他程序占用。

---

## 8.5 文件拖拽模块

### 8.5.1 功能说明

用户将文件或文件夹拖入悬浮窗后，工具自动复制路径。

### 8.5.2 支持拖入类型

- 单个文件；
- 多个文件；
- 文件夹；
- 文件和文件夹混合。

### 8.5.3 默认行为

第一版默认行为：

- 单文件：复制完整路径；
- 多文件：每行一个路径；
- 文件夹：复制文件夹路径。

### 8.5.4 示例

拖入单文件：

```text
D:\素材\首页.png
```

剪贴板内容：

```text
D:\素材\首页.png
```

拖入多个文件：

```text
D:\素材\首页.png
D:\素材\详情页.png
D:\素材\登录页.png
```

剪贴板内容：

```text
D:\素材\首页.png
D:\素材\详情页.png
D:\素材\登录页.png
```

### 8.5.5 拖拽反馈

拖入过程中：

- 悬浮窗变大；
- 边框高亮；
- 显示“松开复制路径”；
- 鼠标移出后恢复。

### 8.5.6 技术实现

WPF 中使用：

```xml
AllowDrop="True"
Drop="Window_Drop"
DragEnter="Window_DragEnter"
DragLeave="Window_DragLeave"
```

获取文件：

```csharp
string[] files = (string[])e.Data.GetData(DataFormats.FileDrop);
```

---

## 8.6 路径格式化模块

### 8.6.1 功能说明

对于拖入或识别到的文件路径，支持复制成不同格式。

### 8.6.2 支持格式

#### 完整路径

```text
D:\project\a.png
```

#### 文件名

```text
a.png
```

#### 不带后缀文件名

```text
a
```

#### 文件夹路径

```text
D:\project
```

#### Markdown 图片

```md
![a](D:\project\a.png)
```

#### Markdown 链接

```md
[a](D:\project\a.png)
```

#### HTML 图片标签

```html
<img src="D:\project\a.png" />
```

#### JSON 数组

```json
[
  "D:\\project\\a.png",
  "D:\\project\\b.png"
]
```

#### CSV

```csv
D:\project\a.png,D:\project\b.png
```

### 8.6.3 默认格式设置

设置页面中支持选择默认拖拽复制格式：

- 完整路径；
- 文件名；
- Markdown；
- JSON 数组。

---

## 8.7 文件快捷操作模块

### 8.7.1 功能说明

识别文件路径后，提供文件相关快捷操作。

### 8.7.2 操作按钮

文件路径操作：

- 复制文件；
- 复制路径；
- 复制文件名；
- 打开文件；
- 打开所在目录；
- 复制 Markdown 链接；
- 复制 HTML 标签。

文件夹路径操作：

- 复制路径；
- 打开文件夹；
- 在终端打开；
- 在 VSCode 打开；
- 在 Cursor 打开；
- 复制文件夹名称。

### 8.7.3 打开所在目录

打开文件所在目录，并选中文件。

Windows 可使用：

```text
explorer.exe /select,"D:\资料\报告.pdf"
```

### 8.7.4 打开文件夹

打开目录：

```text
explorer.exe "D:\资料"
```

### 8.7.5 使用 VSCode 打开

如果本地安装 VSCode：

```text
code "D:\project"
```

### 8.7.6 使用 Cursor 打开

如果本地安装 Cursor：

```text
cursor "D:\project"
```

---

## 8.8 托盘模块

### 8.8.1 功能说明

程序关闭窗口时不直接退出，而是最小化到系统托盘。

### 8.8.2 托盘菜单

托盘右键菜单包含：

```text
显示悬浮窗
隐藏悬浮窗
设置
开机启动
关于
退出
```

### 8.8.3 双击托盘图标

双击托盘图标：

- 如果悬浮窗隐藏，则显示；
- 如果悬浮窗显示，则隐藏。

---

## 8.9 设置模块

### 8.9.1 功能说明

设置页面用于控制工具行为。

### 8.9.2 基础设置

支持配置：

- 是否开机启动；
- 是否启动后显示悬浮窗；
- 是否始终置顶；
- 是否启用剪贴板监听；
- 是否启用拖拽复制；
- 是否显示操作成功提示。

### 8.9.3 剪贴板设置

支持配置：

- 是否监听剪贴板；
- 是否识别文件路径；
- 是否识别文件夹路径；
- 是否识别图片路径；
- 是否忽略不存在路径；
- 监听间隔；
- 防重复提示时间。

### 8.9.4 拖拽设置

支持配置：

- 拖入文件后的默认行为；
- 多文件路径分隔符；
- 是否显示拖拽确认；
- 是否自动复制。

### 8.9.5 外观设置

支持配置：

- 主题模式；
- 透明度；
- 圆角大小；
- 悬浮窗大小；
- 悬浮窗位置；
- 是否启用动画。

---

## 9. 第二阶段功能规划

## 9.1 图片路径预览

### 功能说明

当识别到图片路径时，悬浮窗直接显示缩略图。

### 支持格式

- png；
- jpg；
- jpeg；
- gif；
- bmp；
- webp；
- svg。

### 操作按钮

- 复制图片；
- 复制文件；
- 复制路径；
- 打开图片；
- 打开目录。

---

## 9.2 图片内容复制

### 功能说明

将图片内容写入剪贴板，而不是文件对象。

区别：

- 复制图片：粘贴后是图片内容；
- 复制文件：粘贴后是文件附件；
- 复制路径：粘贴后是路径文本。

---

## 9.3 剪贴板历史

### 功能说明

记录最近复制过的内容。

### 支持内容

- 文本；
- 文件路径；
- 图片；
- 文件对象；
- URL；
- JSON；
- 颜色值。

### 快捷键

建议快捷键：

```text
Ctrl + Shift + V
```

呼出历史面板。

---

## 9.4 JSON 工具

### 功能说明

复制 JSON 后自动识别，并提供格式化能力。

### 操作

- 格式化 JSON；
- 压缩 JSON；
- 复制字段名；
- 保存为 JSON 文件；
- 转义 JSON；
- 反转义 JSON。

---

## 9.5 颜色识别

### 功能说明

复制颜色值后显示颜色预览。

### 支持格式

- HEX；
- RGB；
- RGBA；
- HSL；
- HSLA。

### 示例

复制：

```text
#3F8CFF
```

显示：

```text
颜色预览：#3F8CFF

[复制 HEX] [复制 RGB] [复制 CSS 变量]
```

---

## 9.6 URL 识别

### 功能说明

复制 URL 后提供快捷操作。

### 操作

- 打开链接；
- 复制 Markdown 链接；
- 下载文件；
- 复制域名；
- 复制页面标题。

---

## 9.7 OCR 识别

### 功能说明

拖入图片后进行 OCR 文字识别。

### 场景

- 错误截图提取；
- 文档截图转文字；
- 表格截图识别；
- 图片中的路径识别。

第一版不做 OCR，后续作为增强能力。

---

## 9.8 批量重命名

### 功能说明

拖入多个文件后，支持批量重命名。

### 操作

- 添加前缀；
- 添加后缀；
- 自动编号；
- 按时间重命名；
- 替换文件名文本；
- 删除指定字符。

---

## 9.9 开发者路径增强

### 功能说明

针对开发项目目录，支持路径转换。

### 示例

原路径：

```text
D:\project\src\views\home\index.vue
```

转换为：

```text
@/views/home/index.vue
```

或者：

```text
src/views/home/index.vue
```

### 配置项

支持配置项目根目录：

```text
D:\project
```

支持配置别名：

```text
@ = D:\project\src
```

---

## 10. UI 设计规范

### 10.1 整体风格

设计方向：

- 现代；
- 轻量；
- 简洁；
- 小工具感；
- 悬浮感；
- 不打扰；
- 操作高效。

### 10.2 视觉关键词

- 圆角卡片；
- 半透明背景；
- 轻阴影；
- 毛玻璃；
- 深色模式；
- 蓝色主色；
- 小图标；
- 清晰按钮。

### 10.3 推荐颜色

主色：

```text
#3F8CFF
```

背景深色：

```text
#1F1F1F
```

卡片背景：

```text
#2B2B2B
```

文字主色：

```text
#FFFFFF
```

文字次级：

```text
#BFBFBF
```

警告色：

```text
#FFB020
```

错误色：

```text
#FF4D4F
```

成功色：

```text
#52C41A
```

### 10.4 悬浮窗尺寸建议

默认小球：

```text
48px × 48px
```

展开卡片：

```text
320px × 180px
```

拖拽状态：

```text
360px × 220px
```

设置页：

```text
720px × 520px
```

### 10.5 动效建议

支持：

- 淡入；
- 淡出；
- 缩放；
- 拖拽高亮；
- 操作成功提示；
- 卡片展开动画。

---

## 11. 项目结构建议

```text
ClipPilot/
│
├── App.xaml
├── App.xaml.cs
│
├── Views/
│   ├── MainWindow.xaml
│   ├── MainWindow.xaml.cs
│   ├── FloatWindow.xaml
│   ├── FloatWindow.xaml.cs
│   ├── SettingsWindow.xaml
│   └── SettingsWindow.xaml.cs
│
├── Services/
│   ├── ClipboardService.cs
│   ├── DragDropService.cs
│   ├── FileActionService.cs
│   ├── TrayService.cs
│   ├── StartupService.cs
│   └── SettingsService.cs
│
├── Models/
│   ├── ClipboardItem.cs
│   ├── FilePathItem.cs
│   ├── AppSettings.cs
│   └── PathFormatType.cs
│
├── Utils/
│   ├── PathHelper.cs
│   ├── ClipboardHelper.cs
│   ├── FileHelper.cs
│   └── JsonHelper.cs
│
├── Assets/
│   ├── Icons/
│   └── Images/
│
└── README.md
```

---

## 12. 核心技术点

### 12.1 剪贴板文本读取

```csharp
var text = Clipboard.GetText();
```

### 12.2 写入文本剪贴板

```csharp
Clipboard.SetText("D:\\test\\a.png");
```

### 12.3 写入文件对象剪贴板

```csharp
StringCollection files = new StringCollection();
files.Add(@"D:\test\a.png");
Clipboard.SetFileDropList(files);
```

### 12.4 判断文件是否存在

```csharp
File.Exists(path);
```

### 12.5 判断文件夹是否存在

```csharp
Directory.Exists(path);
```

### 12.6 始终置顶

```csharp
Topmost = true;
```

### 12.7 拖拽文件

```csharp
string[] files = (string[])e.Data.GetData(DataFormats.FileDrop);
```

### 12.8 打开文件所在目录

```csharp
Process.Start("explorer.exe", $"/select,\"{filePath}\"");
```

---

## 13. 开发计划

### 13.1 第 1 天

目标：

- 创建 WPF 项目；
- 创建主窗口；
- 创建悬浮窗；
- 实现窗口无边框；
- 实现始终置顶；
- 实现窗口拖动。

产出：

- 可以运行的悬浮窗。

### 13.2 第 2 天

目标：

- 实现文件拖拽；
- 拖入文件后读取路径；
- 自动写入剪贴板；
- 显示复制成功提示。

产出：

- 拖入文件即可复制路径。

### 13.3 第 3 天

目标：

- 实现剪贴板监听；
- 读取剪贴板文本；
- 判断文件路径；
- 判断文件夹路径；
- 弹出操作卡片。

产出：

- 复制路径后自动识别。

### 13.4 第 4 天

目标：

- 实现路径转真实文件复制；
- 实现打开文件；
- 实现打开所在目录；
- 实现复制文件名；
- 实现复制 Markdown 链接。

产出：

- 文件路径增强操作可用。

### 13.5 第 5 天

目标：

- 实现托盘；
- 实现退出程序；
- 实现隐藏和显示悬浮窗；
- 实现基础设置；
- 实现开机启动。

产出：

- 第一版可日常使用。

---

## 14. 第一版验收标准

### 14.1 基础验收

必须满足：

- 程序可正常启动；
- 悬浮窗始终置顶；
- 悬浮窗可拖动；
- 拖入文件后可复制路径；
- 拖入多个文件后可复制多行路径；
- 复制文件路径后可识别；
- 点击复制文件后可粘贴真实文件；
- 点击打开目录后能打开资源管理器；
- 程序可最小化到托盘；
- 程序可正常退出。

### 14.2 稳定性验收

必须满足：

- 不重复弹窗；
- 不频繁打扰用户；
- 路径不存在时不崩溃；
- 网络路径不可访问时不崩溃；
- 剪贴板被占用时不崩溃；
- 拖入大量文件时不崩溃。

### 14.3 体验验收

建议满足：

- 操作成功有提示；
- 失败有提示；
- 悬浮窗不会遮挡太多内容；
- 用户可以隐藏悬浮窗；
- 用户可以关闭监听；
- 默认行为简单直接。

---

## 15. 风险与注意事项

### 15.1 杀毒软件误报

由于工具涉及：

- 剪贴板监听；
- 开机启动；
- 悬浮窗；
- 托盘常驻；

可能被部分安全软件关注。

应避免：

- 程序加壳；
- 偷偷上传剪贴板内容；
- 不透明权限行为；
- 未经用户同意开机启动。

### 15.2 剪贴板冲突

剪贴板可能被其他程序占用。

需要：

- try-catch；
- 延迟重试；
- 操作失败提示。

### 15.3 管理员权限问题

如果其他程序以管理员身份运行，而本工具不是管理员权限，拖拽可能失效。

建议：

- 默认不以管理员身份运行；
- 在说明中提示权限差异；
- 必要时提供管理员模式启动选项。

### 15.4 路径安全问题

不要自动执行未知路径。

所有危险操作需要用户确认。

第一版只做：

- 打开文件；
- 打开目录；
- 复制文件；
- 复制路径。

---

## 16. 版本规划

### 16.1 V1.0

核心目标：

- 悬浮窗；
- 拖入复制路径；
- 剪贴板识别路径；
- 路径转真实文件复制；
- 托盘；
- 基础设置。

### 16.2 V1.1

增强目标：

- 图片路径预览；
- 图片内容复制；
- 多格式复制；
- 快捷键；
- 主题设置。

### 16.3 V1.2

增强目标：

- 剪贴板历史；
- JSON 格式化；
- 颜色识别；
- URL 识别。

### 16.4 V2.0

增强目标：

- OCR；
- 批量重命名；
- 开发者路径转换；
- 插件系统；
- 自动化动作。

---

## 17. 后续商业化方向

### 17.1 免费基础版

包含：

- 拖入复制路径；
- 路径识别；
- 复制文件；
- 打开目录。

### 17.2 专业版

包含：

- 剪贴板历史；
- OCR；
- 批量重命名；
- 开发者路径增强；
- 自定义规则；
- 插件能力。

### 17.3 面向人群

适合：

- 开发者；
- 测试人员；
- 设计师；
- 办公人群；
- 文件处理重度用户。

---

## 18. 当前开发原则

第一版开发原则：

- 不做大而全；
- 不做复杂 AI；
- 不做云同步；
- 不做账号系统；
- 不做多平台；
- 优先 Windows；
- 优先本地能力；
- 优先稳定可用；
- 优先小而顺手。

---

## 19. 当前版本目标总结

当前版本要解决的核心问题：

> 用户复制或拖入文件路径后，可以通过悬浮工具快速完成复制路径、复制文件、打开目录等操作。

第一版最重要的三个功能：

1. 拖入文件后自动复制路径；
2. 复制文件路径后识别并弹出操作；
3. 点击后把路径转换成真实文件复制。

只要这三个功能做好，这个工具就已经具备实际使用价值。
